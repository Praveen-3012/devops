apiVersion: v1
kind: Service
metadata:
  name: devops-demo-app-service
  labels:
    app: devops-demo-app
spec:
  type: NodePort
  selector:
    app: devops-demo-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
